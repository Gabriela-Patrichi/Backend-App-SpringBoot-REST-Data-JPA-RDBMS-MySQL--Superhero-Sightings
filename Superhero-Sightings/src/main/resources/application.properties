server.port=7474

spring.datasource.driverClassName= com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/superhero_sightings
spring.datasource.username=root
spring.datasource.password=ilikeMYSQL2

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.ddl-auto = validate
# Validate (recommended in production)-  ORM validates the mapping against the tables already created in the DB


# the below 2 properties will show the sql queries generated by the ORM framework in the console
# however in production these will not be used  -- commented out
#spring.jpa.show-sql= true
#spring.jpa.properties.hibernate.format_sql = true;