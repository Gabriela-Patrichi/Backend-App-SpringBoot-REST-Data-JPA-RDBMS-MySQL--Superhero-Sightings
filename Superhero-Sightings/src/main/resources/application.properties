server.port=7474

spring.datasource.driverClassName= com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/superhero_sightings
spring.datasource.username=root
spring.datasource.password=ilikeMYSQL2

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.ddl-auto = validate

#could have used create - (telling the ORM framework to create the tables based on the mapping, however not Recommended!)
#              or update
#              create-drop
# Validate (recommended , in production)- heer the tables should already be created on the DB and ORM validates the mapping against the tables in the DB

#for new learners we might want to see the SQl queries generated by the ORM framework
# so the below 2 properties will show the sql queries in the console
# however in production these will not be used
spring.jpa.show-sql= true
spring.jpa.properties.hibernate.format_sql = true;